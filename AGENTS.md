# 다먹자 공통 작업 지침

## 목적과 적용 범위

이 파일은 BE·FE·AI·Cloud 모든 파트의 에이전트가 가장 먼저 읽는 공통 진입점이다. 서비스 기준 문서, API 명세, 확정된 논의 결론을 이 wiki 레포 한 곳에서 관리하고, 각 파트 레포는 이 문서를 참조한다. 파트별 코딩·아키텍처·커밋 규칙은 각 파트 레포에서 관리한다.

## 레포 배치

wiki 레포와 작업 레포를 같은 상위 폴더에 나란히 클론한다.

```text
<작업 폴더>/
├── KTB4-5th-wiki/               # 이 레포 (공통 문서)
│   ├── AGENTS.md                # 공통 진입점 (이 파일)
│   └── docs/
│       ├── context/PROJECT_CONTEXT.md    # 서비스 기준 문서 (버전별 기능 분류 포함)
│       ├── api/api-spec.md               # API 명세 사본
│       ├── decisions/issue-decisions.md  # 기능 논의 이슈 최종 결론 모음
│       ├── agents/                       # AI 에이전트 역할별 지침
│       └── templates/part-repo-AGENTS.md # 파트 레포용 AGENTS.md 템플릿
├── <BE 레포>/AGENTS.md          # "../KTB4-5th-wiki/AGENTS.md를 먼저 읽는다"
├── <FE 레포>/AGENTS.md
└── ...
```

파트 레포의 `AGENTS.md`는 [파트 레포 템플릿](docs/templates/part-repo-AGENTS.md)을 기준으로 작성한다.

## 작업별 필독 문서

| 작업 | 읽을 문서 | 해당 문서가 관리하는 기준 |
|---|---|---|
| 기능·정책 파악, 설계, 구현 방향 확인 | [PROJECT_CONTEXT](docs/context/PROJECT_CONTEXT.md) | 서비스 정의, 버전별 기능 범위, 도메인 정책, 저장소·DB 제약, 미결 사항 |
| API 구현·연동, 요청·응답 확인 | [API 명세](docs/api/api-spec.md) | 경로, 요청·응답 형식, 에러 코드, 열거값, 명세 검토 메모 |
| 논의로 확정된 정책 확인 | [결정 모음](docs/decisions/issue-decisions.md) | wiki 이슈의 최종 결론과 기준 문서와의 차이 |
| AI 에이전트로 이슈·문서·진행 상황을 다룰 때 | [에이전트 구조](docs/agents/README.md) | 역할별 입력·출력·금지 사항, 작업 흐름 |
| 브랜치·커밋·PR, 코딩 규칙 | 각 파트 레포의 `docs/conventions/` | 파트별로 관리 |

여러 종류의 작업이 포함되면 관련 문서를 모두 읽는다. 요약본과 원문이 다르면 원문(결정 모음 → 해당 이슈 댓글의 최종 결론)을 확인하고 차이를 알린다.

## 무엇을 어디에 남기나

| 종류 | 위치 |
|---|---|
| 기능 논의·정책 결정 | wiki 이슈 (`기능 논의` 템플릿, 댓글에 "최종 결론") |
| 확정된 결과 | wiki 문서 (PR로 반영, PR 본문에 논의 이슈 링크) |
| 구현 작업·버그 | 각 파트 레포 이슈 |
| 태스크 진행 상태 | GitHub Projects 보드 상태 필드 (`To do` / `In Progress` / `Done`) |

## 공통 작업 원칙

- 새 대화나 컨텍스트 시작 시 Notion·Figma·ERDCloud·구글 시트 링크를 자동으로 열지 않는다. 사용자가 조회·동기화를 요청하거나, 작업에 꼭 필요한 정보가 이 레포 문서에 없을 때만 필요한 부분을 확인한다.
- 합의된 내용만 확정 규칙으로 적용한다. 미정은 미정으로 기록하고, 에이전트의 추천안을 팀 규칙으로 만들지 않는다.
- 문서와 실제 코드·명세·이슈 결론이 충돌하면 구체적인 차이를 알린다. 임의로 한쪽을 고쳐 충돌을 해소하지 않는다.
- 요청한 기능의 버전 범위를 PROJECT_CONTEXT의 버전별 기능 분류에서 확인한다. 범위 밖이거나 분류가 `확인 필요`이면 구현 전에 알린다.
- 사용자가 문서 작업만 요청하면 문서만 수정한다.

## AI와 사람의 역할

| 사람 (팀) | AI |
|---|---|
| 방향·정책 결정 | 관련 이슈·문서 찾아 연결 |
| 태스크 분할과 일정 산출 | 이슈 내용과 문서의 불일치 확인 |
| 담당자 배정, 구현, 테스트, 리뷰 | 결정 사항을 문서 초안으로 정리 (반영은 PR 리뷰 후) |
| 문서 반영 PR 승인 | 진행 상황 요약 (보드 상태 기준, 판단 없이 사실만) |

- AI는 이슈를 쪼개거나 일정·담당자를 정하지 않는다.
- AI는 `main`에 직접 반영하지 않고, 이슈 댓글·PR 초안으로만 결과를 남긴다.
- 커밋은 코드 단위이며 태스크·일정 단위와 억지로 맞추지 않는다.

## 문서 관리

- 같은 규칙은 위 표의 기준 문서 한 곳에서 관리하고 다른 문서는 링크로 참조한다.
- 확정된 결론은 [결정 모음](docs/decisions/issue-decisions.md)에 먼저 추가하고, 기준 문서·API 명세에 반영이 필요한 차이는 결정 모음 하단 대조표에 기록한다.
- 외부 원본(Notion·구글 시트)을 동기화한 문서는 상단의 스냅샷 일자를 갱신한다.

## 완료 보고

- 변경 내용, 수행한 검증과 결과, 미실행 검증과 그 이유, 남은 결정 사항을 간단히 보고한다.
- 문서만 바꾼 경우 링크·경로·용어의 일관성을 확인한다. 수행하지 않은 검증을 성공으로 보고하지 않는다.
